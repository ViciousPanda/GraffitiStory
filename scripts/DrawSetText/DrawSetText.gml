/// @desc DrawSetText(color,fount,halign,valign)
/// @arg color
/// @arg font
/// @arg halign
/// @arg valign

/// allows for one line of major text drawing vars
/// requireing all four prevents silly code mistake of not closing

draw_set_color(argument0);
draw_set_font(argument1);
draw_set_halign(argument2);
draw_set_valign(argument3);