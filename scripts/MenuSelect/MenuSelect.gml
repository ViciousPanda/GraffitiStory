/// @desc MenuSelect
/// @desc Activates the selected menu option
		
		menu_x_target = gui_width + 200;
		menu_committed = menu_cursor;
		menu_control = false;
		audio_play_sound(snMenu,2,false);